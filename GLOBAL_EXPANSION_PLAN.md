# GLOBAL_EXPANSION_PLAN.md — международная версия aineron

Дата: 2026-07-07. Статус: утверждённый план реализации.

---

## 0. Зафиксированные решения

| Решение | Значение |
|---------|----------|
| Домен | Отдельный международный домен (aineron.com / .ai / .app — купить) |
| Инфраструктура | **Второй сервер вне РФ**, полностью отдельный инстанс (код общий, деплой и БД свои) |
| Оплата | **Только криптовалюта** на старте (USDT/TON), позже — Telegram Stars и локальные PSP |
| Валюта продукта | **Кредиты** (внутренне = те же kopecks, 1 кредит = 1 внутренняя единица) |
| Языки | **Английский — базовый и обязательный** + языки целевых рынков (см. §2). Русского на международной версии НЕТ — Россию и СНГ обслуживает aineron.ru |
| Исключённые рынки | Франция, Германия, ЕС в целом, США, Великобритания, Китай — «жирные» конкурентные рынки без нашего преимущества |
| Под вопросом (волна 2) | Индия, Пакистан/Бангладеш, Вьетнам — решение после метрик волны 1 |

Логика выбора рынков: идём туда, где есть **гео-барьер** (AI-сервисы заблокированы) или **платёжный барьер** (местной картой нельзя оплатить ChatGPT), слабый английский и слабая конкуренция. Это в точности та формула, на которой выросли в России.

---

## 1. Целевые рынки

### СНГ (обслуживается существующим aineron.ru, НЕ международной версией)

Страны: **Казахстан, Узбекистан, Беларусь, Кыргызстан, Таджикистан, Азербайджан, Армения** (+ русскоязычные в Грузии и Молдове).

- Русский интерфейс уже покрывает эту аудиторию (~60–80 млн). Отдельная локализация не нужна.
- Единственная доработка на .ru: **приём крипты** (та же интеграция, что и для международной версии — делаем один раз, включаем на обоих инстансах) и позже Kaspi (KZ) / Payme, Click (UZ).
- Маркетинг: Telegram-каналы и таргет по KZ/UZ — бот работает у них уже сейчас.

### Волна 1 — международная версия

| Приоритет | Рынок | Язык | Аудитория | Наш барьер-моат |
|-----------|-------|------|-----------|-----------------|
| 1 | **Иран** | fa (фарси) | 85 млн | Полная блокировка OpenAI/Google/Anthropic — точный клон российского кейса. Telegram — главный мессенджер. Карты не работают в принципе → крипта безальтернативна. Конкуренция ~0. |
| 2 | **Турция** | tr | 85 млн | Турецкие карты массово отклоняются зарубежными подписками, лира волатильна. Слабый английский, высокая цифровизация. |
| 3 | **Индонезия** | id | 280 млн | Низкое проникновение международных карт, очень слабый английский, mobile-first. Локализованных AI-агрегаторов почти нет. |
| 4 | **Египет + MENA** | ar | 400+ млн (весь арабский мир одним переводом) | Платёжный барьер, слабый английский. Требует RTL. |
| 5 | **Нигерия** | en | 220 млн | Валютные лимиты найры, карты не проходят. Английский уже есть → рынок открывается «бесплатно» вместе с en-версией. Высочайшая крипто-адопция — идеально под нашу оплату. |

### Волна 2 — по результатам волны 1 (не делаем сейчас)

| Рынок | Язык | Почему отложено |
|-------|------|-----------------|
| Вьетнам | vi | Рынок хороший (платёжный барьер, слабый английский), но добавление языка дешевле делать после обкатки пайплайна переводов. |
| Пакистан, Бангладеш | ur, bn | Огромный объём, но очень низкий ARPU — сначала проверить юнит-экономику на Иране/Индонезии. |
| Индия | en + hi | Англоязычная элита уже на ChatGPT (он доступен, UPI работает); hindi-масс-маркет — низкий ARPU. en-версия и так будет им доступна — посмотрим на органику из Индии и решим. |
| Латам | es, pt-BR | ChatGPT доступен, барьер только частичный (Аргентина). Добавить, если появится органический спрос. |

**Итого языков волны 1: en (базовый), fa, tr, id, ar.** Пять локалей.

---

## 2. Архитектура: два независимых инстанса

```
aineron.ru  (сервер РФ, как сейчас)          aineron.com (новый сервер, Hetzner/аналог, ЕС)
├── рубли, Robokassa + Stars + крипта        ├── кредиты, только крипта
├── язык: ru                                 ├── языки: en, fa, tr, id, ar
├── своя БД                                  ├── СВОЯ БД (пользователи не пересекаются)
└── деплой как сейчас                        └── тот же репозиторий, свой .env + compose
```

Принципы:

- **Один репозиторий, два деплоя.** Никакого форка кода — международное поведение включается env-флагами. Иначе через три месяца будет два расходящихся проекта.
- Новые env-переменные:
  ```
  INTL_MODE=1                  # международный инстанс
  DEFAULT_LOCALE=en
  ENABLED_LOCALES=en,fa,tr,id,ar
  CURRENCY_DISPLAY=credits     # credits | rub
  PAYMENTS_ENABLED=crypto      # список каналов: crypto | robokassa | stars
  ```
- `INTL_MODE=1` отключает: Robokassa, юр-страницы РФ, русскоязычные management-команды контента; включает: крипто-чекаут, кредитную витрину, мультиязычный роутинг.
- Сервер: вне РФ (Hetzner / Contabo / OVH). Причины: доступность для Ирана и Турции (российские IP-диапазоны там нестабильны), доверие аудитории, независимость от РФ-инфраструктуры. Провайдеры laozhang.ai / apimart.ai работают с любого сервера.
- Docker Compose тот же (web, celery, redis, db, frontend, nginx); Studio/Gitea-сервисы на международной версии на старте можно не поднимать (`STUDIO_ENABLED=0`) — меньше поверхности, меньше ресурсов.
- Юр-тексты: privacy policy / terms на английском, нейтральная юрисдикция; `LegalDocument` уже управляется из админки — просто другой контент.

---

## 3. Биллинг: кредиты + крипта

### 3.1. Кредиты

Внутри ничего не меняем — `balance_kopecks`, идемпотентный ledger, `spend_kopecks`/`add_kopecks` работают как есть. Меняется только **витрина**:

- 1 кредит = 1 внутренняя единица (бывшая копейка). Цены моделей уже лежат в `cost_kopecks` — на международной версии это просто «кредиты за сообщение».
- `frontend/lib/money.ts` → добавить `formatCredits(amount, locale)`; выбор `formatRub`/`formatCredits` по `CURRENCY_DISPLAY`.
- Пакеты пополнения в USD-номинале: например $5 / $10 / $25 / $50 → кредиты по фиксированному курсу пакета (не по биржевому курсу рубля — международная цена назначается заново, исходя из себестоимости токенов в $; это отдельная строка в TARIFFS-экономике).
- Слово «звёзды» на международной версии не используется нигде (конфликт с Telegram Stars).

### 3.2. Крипто-платежи (единственный канал на старте)

Двухступенчатая стратегия:

**Шаг 1 — Crypto Pay API (@CryptoBot).** Запуск за 1–2 дня:
- REST API: создать инвойс (USDT/TON/BTC/ETH) → получить ссылку → редирект/кнопка.
- Webhook подтверждения с подписью.
- Работает и на сайте (ссылка-инвойс), и в боте нативно.
- Комиссия ~1–3%; вывод в USDT/TON.

**Шаг 2 — прямой приём USDT TRC-20 / TON** (когда пойдёт объём): собственные депозит-адреса или memo-инвойсы, индексация сети, ноль посредников и комиссий. Отдельный мини-проект, не блокирует запуск.

Интеграция в наш биллинг (по образцу Robokassa):

```
src/users/crypto_payments.py
  create_crypto_invoice(user, usd_amount) → PaymentHistory(status='pending',
      payment_type='crypto', external_id=invoice_id)

POST /users/api/payment/crypto/callback/   (webhook)
  проверка подписи → user.add_kopecks(credits, type='topup',
      reference=f'crypto:{invoice_id}')      # идемпотентно из коробки
```

- Инвойс фиксирует сумму кредитов в момент создания, TTL 30 минут.
- Тот же модуль включаем и на aineron.ru (пополнение криптой для СНГ) — одна интеграция, два инстанса.
- UI: страница `/account/billing/` — блок «Pay with crypto» (USDT TRC-20 / TON / BTC), статус-поллинг инвойса, как сейчас у Robokassa-флоу.

---

## 4. Мультиязычность — как делать правильно

### 4.1. Frontend: next-intl

Стандарт де-факто для Next.js 14 App Router — **next-intl**. Что делается:

1. **Реструктуризация роутинга**: `frontend/app/*` → `frontend/app/[locale]/*` (лендинг, каталог, чат, кабинет, блог и т.д.). Технические файлы (`manifest.ts`, `robots.ts`, `api`-роуты) остаются снаружи.
2. **middleware.ts**: определение локали — путь → cookie `NEXT_LOCALE` → заголовок `Accept-Language` → `en`. Редирект `/` → `/en/` (или serve default без префикса — выбрать `localePrefix: 'always'`, для hreflang чище).
3. **Словари**: `frontend/messages/en.json`, `fa.json`, `tr.json`, `id.json`, `ar.json` — формат ICU Message Format (плюрали, подстановки):
   ```json
   { "chat": { "insufficientBalance": "Not enough credits. You need {count, plural, one {# credit} other {# credits}}." } }
   ```
4. **Извлечение строк** — самая трудоёмкая часть: все хардкод-строки в `app/` и `components/` заменить на `t('key')`. Делать постранично, начиная с денежного пути: лендинг → регистрация → каталог → чат → биллинг. Строки глубоких настроек можно переводить второй итерацией.
5. **generateMetadata** per-locale (title/description из словаря), `sitemap.ts` с `alternates.languages` (hreflang), JSON-LD на языке страницы.
6. **CI-проверка**: скрипт, падающий при недостающих ключах в любой локали (сравнение ключей с en.json) — добавить рядом с `check_no_emoji.py`.

### 4.2. RTL и шрифты (критично для fa и ar)

- `<html lang={locale} dir={locale === 'fa' || locale === 'ar' ? 'rtl' : 'ltr'}>` в корневом layout.
- Tailwind: перейти на **logical properties** — `ms-*/me-*` вместо `ml-*/mr-*`, `ps-*/pe-*` вместо `pl-*/pr-*`, `text-start/text-end` вместо `text-left/right`. Tailwind сам разворачивает их в RTL. Пройтись поиском по `ml-|mr-|pl-|pr-|left-|right-|text-left|text-right` и заменить там, где это направление текста, а не абсолютное позиционирование.
- Шрифты: латиница/кириллица — Inter (как сейчас); **fa — Vazirmatn**; **ar — IBM Plex Sans Arabic или Noto Naskh Arabic**. Подключить через `next/font` с `subsets`, выбирать по локали в layout.
- Компоненты со «направленной» логикой (карусели, стрелки «назад», чат-пузыри) — проверить вручную на fa.

### 4.3. Backend (Django/DRF)

- **Ошибки и сообщения API → коды, не текст.** `{"error": {"code": "insufficient_balance", "params": {"required": 500}}}` — текст рендерит фронт из словаря. Это надёжнее и дешевле, чем gettext по всему DRF, и не требует передачи локали в Celery-задачи.
- **Переводимый контент БД** — `django-modeltranslation` (колонки `field_en`, `field_fa`, ...):
  - `NeuralNetwork`: `description`, `seo_title`, `seo_description`
  - `FAQ`: `question`, `answer`
  - `Tariff`/пакеты: `name`, `description`
  - `Category`: `name`
  - API отдаёт язык по `?lang=` или `Accept-Language` (fallback: en).
- **Блог**: НЕ переводить посты через modeltranslation. SEO-статьи на каждом языке — это отдельные статьи под локальные ключевики (см. §4.5). Добавить `Post.language` + фильтр в API.
- Транзакционные email: шаблоны per-locale, у пользователя поле `language` (заполняется при регистрации из локали фронта).

### 4.4. Чем переводить — пайплайн

Целевые языки (fa, ur, bn — вне покрытия DeepL; ar/tr/id DeepL умеет, но без контекста продукта), поэтому:

**Основной инструмент — LLM-перевод через собственный доступ (laozhang.ai), моделью уровня Claude Sonnet / GPT-4o.** Преимущества: покрывает все наши языки, понимает контекст («credits» ≠ «титры»), сохраняет ICU-плейсхолдеры, стоит копейки на нашем же канале.

Пайплайн (оформить management-командой `translate_locales`):

1. **Источник истины — en.json** (написать английскую версию вручную/с вычиткой — это база, с которой переводится всё).
2. **Глоссарий** `frontend/messages/_glossary.json`: фиксированные термины — credits, top up, prompt, model, названия продуктов (не переводятся), tone of voice. Подаётся в system prompt переводчика.
3. Скрипт: для каждого ключа, отсутствующего в `{locale}.json`, — батч-перевод LLM с контекстом (`ключ`, `en-строка`, `глоссарий`, `описание экрана`). Инкрементально: переводятся только новые/изменённые ключи (хранить hash en-строки).
4. **Валидация автоматом**: плейсхолдеры `{count}` сохранены, ICU-синтаксис парсится, длина строки не взорвала кнопку (soft-лимит ×1.6 от en).
5. **Вычитка носителем — обязательна для денежного пути** (лендинг, прайсинг, чекаут, онбординг) на fa/tr/id/ar. Заказывается на Upwork/локальных биржах, объём небольшой (~200–400 строк). Остальной интерфейс идёт машинным переводом и правится по жалобам.
6. Тот же пайплайн — для контента БД (`translate_catalog`): описания моделей, FAQ.

Что НЕ делать: не подключать Crowdin/Lokalise на старте (оверхед для соло-команды; JSON в git + LLM-пайплайн покрывает всё), не использовать Google Translate API (качество маркетинговых строк заметно хуже LLM).

### 4.5. SEO на локальных языках

Главный канал органики — на fa/tr/id/ar контентная конкуренция в AI-нише в разы ниже, чем в en:

- Расширить существующую контент-машину (`create_seo_posts`) параметром языка: локальные ключевики («ChatGPT بدون فیلتر», «yapay zeka türkçe», «AI tanpa VPN») → 100–200 программных статей на язык.
- hreflang-связки между локалями + x-default=en.
- Страницы моделей `/[locale]/models/[slug]/` с переведёнными описаниями и JSON-LD.
- Иран: Google работает через фильтр, но им пользуются; плюс локальные каналы дистрибуции — Telegram-каналы важнее SEO.

### 4.6. Telegram-бот международной версии

- **Отдельный бот** (@aineron_global_bot или per-market боты) на международном инстансе — не смешивать с русским.
- Локаль: `from_user.language_code` + выбор в /settings, поле `TelegramUser.language`.
- Строки хендлеров → словарь `telegram_bot/locales/{en,fa,tr,id,ar}.json` + хелпер `t(user, key)` (aiogram 3 позволяет и `aiogram.utils.i18n` с gettext, но JSON-словарь единообразнее с фронтовым пайплайном — переводится той же командой).
- Оплата в боте: инвойсы Crypto Pay (нативно в Telegram) — для Ирана бот, вероятно, станет основным интерфейсом, важнее сайта.

---

## 5. Фазы работ

| Фаза | Содержание | Оценка |
|------|-----------|--------|
| **G1. Крипто-платежи** | ГОТОВО (a1f619f, проверено в проде 2026-07-07, приложение «aineron»; временно выключено флагом CRYPTO_PAY_ENABLED=0 — включается одной переменной без пересборки) | готово |
| **G2. i18n-каркас** | ЗАВЕРШЁН (2026-07-08). Сделано: next-intl в режиме «без роутинга» (локаль инстанса = NEXT_PUBLIC_DEFAULT_LOCALE, ARG в Dockerfile.frontend; .ru собирается как ru и не меняется), словари messages/{ru,en}.json + _glossary.json, LLM-пайплайн `npm run translate-locales` (инкрементальный хэш-кэш, валидация ICU-плейсхолдеров, батчи через laozhang; фикс бага unflatten() — ломал массивы в en.json), CI `npm run check-locales`, RTL-атрибут dir в root layout. Локализовано: Footer/лендинг/auth/каталог/кабинет/биллинг, весь чат (`app/chat/[chatId]/page.tsx` + 20 файлов `components/chat/*.tsx`), блок из 22 файлов (account/*, compare, arena, personas, projects list, prompts, gallery, welcome, dashboard/*, status, blog, privacy-policy/terms UI), ru-RU date/number locale fix + 2 кросс-доменных бага (хардкод aineron.ru в share-ссылке и API base_url), 2 публичные share-страницы (s/[slug], g/[slug]), `projects/[id]/page.tsx` (2244 строки), `api-docs/*` (~250 ключей), коды ошибок API (`api/error_messages.py`, 115 сообщений/12 файлов), 39 встроенных шаблонов промтов (modeltranslation), `/sandbox` (landing Sandbox API), `/docs` (766 строк, 8 разделов пользовательской документации), root-layout `generateMetadata` (был статичным ru-объектом — ломал title/description фоллбэк на `/prompts` и title-суффикс на всех страницах). 2026-07-08: докручена **адаптация** (не только перевод) лендинга (`home`) и `apiDocs` — там ещё оставались буквальные "Servers in Russia, pay in rubles", "Robokassa", "RF offer" в en.json с первой волны перевода, до выработки паттерна адаптации. Итого 1967 ключей ru/en синхронизированы, обе сборки проверены, задеплоено и построчно сверено на живом aineron.net. Сознательно не тронуто: `studio/*` (функция выключена), `tg/*`+`account/telegram` (нет intl-бота), starter-prompt плейсхолдеры в account/projects (пример специально демонстрирует языковое ограничение — не баг), `[locale]`-роутинг (отложено до G4, нужен только когда появится второй недефолтный язык). **Вывод реферальных начислений** — РЕШЕНО (33c6120): `WithdrawalRequest.card_number` → `payout_destination` (max_length 128), UI переключает карта/крипто-кошелёк по `CURRENCY`, .ru не задет. **Вторая волна находок пользователя (2026-07-08, fb33090/05d7447)**: Footer.tsx и Navbar.tsx хардкодили бренд "aineron.ru" и `support@aineron.ru` на КАЖДОЙ странице; `footer.legal` буквально показывал реальные ФИО+ИНН оператора; `app/manifest.ts` (PWA) был полностью на русском с "aineron.ru" в имени; `lib/api/server.ts` слал заголовок `Host: aineron.ru` при SSR-запросах к Django независимо от инстанса (ломало превью-картинки блога и `serverGetLegalDoc` — `/terms`/`/privacy-policy` показывали заглушку вместо реального текста); `fal_utils.py` — водяной знак на бесплатных изображениях был хардкоднут как "aineron.ru" для обоих инстансов; `/business-bot` (Telegram-бот фича, нет на intl) — теперь 404 на credits-инстансе; статус/блог JSON-LD дублировали "aineron.ru" внутри самих строк словаря. Новый `lib/site.ts` (SITE_URL/siteHost/brandName/supportEmail) — общая точка вместо копий по файлам. Всё проверено live-curl на aineron.net: 0 упоминаний aineron.ru/рублей/ФИО/ИНН. | готово |
| **G3. Международный инстанс** | ЗАПУЩЕН 2026-07-07: **https://aineron.net** (HOSTKEY NL, 66.151.32.164, hostname aineron-intl). Сделано: SSH-ключи (пароль отключён), UFW+fail2ban, deploy-ключ GitHub (клон в /opt/aineron), Let's Encrypt + автопродление (webroot + deploy-hook), 8 контейнеров по docker-compose.intl.yml, локаль en, крипта — единственная оплата, Robokassa обезврежена (тест+пустые ключи), посеяно: тарифы, 56 моделей + 13 видео, суперюзер; кредитная витрина вместо ₽ (INTL_MODE); en-контент каталога (modeltranslation, cf96e4b); юр-доки (2026-07-08, 708fa48/9ed4faf) — независимые англ. Terms/Privacy без персональных данных исполнителя, оплата только крипто, `LegalDocument` переведён через modeltranslation (title_ru/title_en, content_ru/content_en), aineron.ru не затронут. Обновление: `cd /opt/aineron && bash deploy_intl.sh`. **Почта — РЕШЕНО (2026-07-08)**: SMTP-порты HOSTKEY остались закрытыми (не открывали), вместо этого ImprovMX + MX/SPF записи в Njalla (реальный DNS-хостинг домена), алиас `*@aineron.net → leha.gutman.83@mail.ru`, статус Active, проверено `nslookup`. support@aineron.net принимает почту. | 1–2 недели |
| **G4. Запуск волны 1** | Локали fa + tr (перевод пайплайном + вычитка носителями, RTL для fa), международный бот en/fa/tr, лендинги с локальным позиционированием | 2 недели |
| **G5. Добор языков** | id, ar (RTL уже готов после fa), SEO-контент-машина на всех языках | 1–2 недели |
| **G6. По метрикам** | Волна 2 (vi/ur/bn/hi/es), Stars как второй платёжный канал, локальные PSP, прямой приём TON | — |

Порядок G1 первым не случаен: крипта полезна уже сейчас на .ru и не зависит ни от чего остального.

**Метрики решения для волны 2** (смотреть после 4–6 недель G4/G5): конверсия визит→регистрация→первое пополнение по странам, ARPU, доля крипто-инвойсов оплаченных/созданных, органика из Индии/Вьетнама на en-версии.

---

## 6. Позиционирование по рынкам

«Без VPN» не переводится дословно — на каждом рынке своя боль:

| Рынок | Сообщение |
|-------|-----------|
| Иран | «Доступ ко всем AI-моделям без фильтров и иностранной карты» |
| Турция | «GPT, Claude, Gemini — оплата без зарубежной карты, дешевле подписки $20» |
| Индонезия | «Semua model AI dalam satu tempat» — все модели в одном месте, на индонезийском, поштучная оплата вместо подписки |
| MENA | «كل نماذج الذكاء الاصطناعي في مكان واحد» — единый доступ + арабский интерфейс |
| Нигерия | «Pay for AI with crypto — no dollar card needed» |

Общий козырь везде: **поштучная оплата вместо $20/мес** — для рынков с низким доходом это сильнее, чем для России.

---

## 7. Риски и что проверить до старта

1. **Иран и санкции**: проверить ToS laozhang.ai/apimart.ai и Crypto Pay на предмет ограничений по юзерам из Ирана; наш сервер в ЕС обслуживает иранский трафик — юр-риски оценить отдельно. Если критично — Иран переводится в Telegram-only режим (мягче поверхность).
2. **Юнит-экономика**: цены в кредитах назначать от $-себестоимости токенов + комиссия крипто-процессинга, а не конвертацией рублёвого прайса. Отдельная вкладка в TARIFFS.md.
3. **Абузы**: бесплатный стартовый грант на международной версии урезать или выдавать после первого пополнения — фермы аккаунтов из целевых стран агрессивнее; `ShadowBanMiddleware` по IP оставить включённым.
4. **Расхождение кодовых баз**: любые фичи международной версии — только через env-флаги в общем репозитории. Раз в неделю деплой обоих инстансов из main.
5. **Поддержка**: саппорт en + автоответчик на базе собственного продукта для fa/tr/id/ar; support@ на международном домене.
6. **Крипто-волатильность**: инвойсы в USDT (стейбл) как дефолт; TON/BTC — по фиксированному на 30 минут курсу.
7. **Вывод реферальных начислений**: РЕШЕНО (2026-07-08, 33c6120) — `payout_destination` вместо `card_number`, крипто-кошелёк на .net вместо карты, .ru не задет (детали в §5, строка G2).

---

## 8. Итоговый статус (2026-07-08) — что сделано и что осталось

### Сделано

- **G1 (крипта)** — задеплоено в прод, работает, временно выключено флагом (включается без пересборки).
- **G2 (i18n)** — завершён полностью: весь UI, документация, коды ошибок, каталог, юр-документы, PWA-манифест переведены и адаптированы (не только переведены — убраны RF-специфичные формулировки: рубли/Robokassa/"только в России" заменены на crypto/credits). Две волны находок пользователя (хардкод aineron.ru в Footer/Navbar/manifest/JSON-LD, реальные ФИО+ИНН оператора в футере, сломанный Host-заголовок при SSR, водяной знак с чужим доменом) — все закрыты и проверены live.
- **G3 (международный инстанс)** — aineron.net запущен, работает, почта настроена (ImprovMX+Njalla), крипто-платежи и реферальный крипто-вывод готовы.
- **Побочный найденный и исправленный баг** (не про i18n, общий для обоих инстансов): свободный тариф не мог написать ни одного сообщения vision-моделям (GPT-4o и большинству современных текстовых моделей с поддержкой фото-вложений) — гейт "медиа только на платных тарифах" ошибочно путал "модель принимает фото/видео" с "модель генерирует фото/видео". Исправлено в `api/views/chats.py` (×2) и `api/views/files.py`, задеплоено на aineron.net (cb95076). **На aineron.ru фикс запушен в `main`, но НЕ задеплоен** — решение о деплое на .ru остаётся за пользователем (см. правило в памяти: не деплоить .ru без явного запроса).

### Осталось

- **Деплой на aineron.ru** накопившихся изменений из `main` (payout_destination, все i18n-фиксы актуальны только косметически для .ru, но фикс vision-моделей — реальный баг, который стоит закатать в прод: он одинаково блокирует бесплатных пользователей и на .ru).
- **G4** — локали fa + tr (перевод + вычитка носителями, RTL для fa), международный Telegram-бот, `[locale]`-роутинг.
- **G5** — id, ar (RTL уже готов после fa), SEO-контент-машина на новых языках.
- **G6** — по метрикам волны 1: Stars как второй платёжный канал, локальные PSP, прямой приём TON, решение по волне 2 рынков.
- **Мелкое, не блокирующее**: `/blocked` (shadow-ban страница) и часть более глубоких настроек ещё не прошли через next-intl (полностью на русском) — низкая видимость, не денежный путь.
