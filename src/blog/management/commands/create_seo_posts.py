"""
Создать две SEO-статьи для блога aineron.ru.
Запуск: python manage.py create_seo_posts
"""
from django.core.management.base import BaseCommand
from django.utils import timezone
from blog.models import Category, Post


ARTICLE_1_CONTENT = """<p>AI-нейросети стали не просто инструментом продуктивности — они изменили способ работы миллионов людей. Но одна проблема долгое время сдерживала российских пользователей: VPN, ограничения, нестабильный доступ, оплата иностранными картами. <strong>aineron.ru</strong> решает все эти проблемы сразу.</p>

<h2>1. Полный доступ без VPN — навсегда</h2>

<p>Это ключевое преимущество, которое объясняет, почему тысячи пользователей выбирают aineron.ru. Вы работаете с GPT-4o, Claude 3.5 Sonnet, Gemini 1.5 Pro, Mistral Large и десятками других моделей <strong>напрямую с российского IP-адреса</strong>.</p>

<p>Никакой нестабильности VPN. Никаких обрывов соединения посередине важного разговора. Никакой зависимости от подписки на VPN-сервис, который сам по себе может быть заблокирован.</p>

<p>Технически это работает через агрегирующий AI-провайдер с серверами за пределами России — вы просто обращаетесь к нашему API, а мы уже обеспечиваем маршрутизацию. Для вас это просто работает.</p>

<h2>2. Все топовые модели в одном месте</h2>

<p>Вместо того чтобы платить за подписки в каждой отдельной компании, aineron.ru даёт доступ к полному каталогу:</p>

<ul>
<li><strong>Текстовые LLM:</strong> GPT-4o, GPT-4 Turbo, Claude 3.5 Sonnet, Claude 3 Opus, Gemini 1.5 Pro, Mistral Large, Llama 3.1 405B и другие</li>
<li><strong>Генерация изображений:</strong> FLUX.1 Schnell, Stable Diffusion XL, DALL-E 3</li>
<li><strong>Генерация видео:</strong> модели для создания коротких видеоклипов из текста или изображения</li>
</ul>

<p>Новые модели появляются в каталоге в течение нескольких дней после релиза — вам не нужно ждать официального открытия для России.</p>

<h2>3. Честная экономика: платите только за то, что используете</h2>

<p>Большинство западных сервисов работают по принципу ежемесячной подписки: вы платите $20–$30 в месяц независимо от того, пользуетесь вы сервисом каждый день или только раз в неделю.</p>

<p>aineron.ru использует систему <strong>звёзд</strong> — внутренней валюты, которую вы тратите только на фактически отправленные сообщения. Это особенно выгодно для тех, кто:</p>

<ul>
<li>Использует нейросети периодически, а не каждый день</li>
<li>Хочет попробовать разные модели без обязательств</li>
<li>Работает по проектам: интенсивно один месяц, тихо в следующий</li>
</ul>

<p>При этом для активных пользователей доступны подписки, дающие звёзды со значительной скидкой — и часть моделей переходят в режим <em>безлимит</em> в рамках тарифа.</p>

<h2>4. Пробный период: 10 звёзд сразу при регистрации</h2>

<p>Любой новый пользователь получает <strong>10 звёзд бесплатно</strong> — без ввода карты, без верификации телефона. Этого достаточно, чтобы написать несколько запросов к GPT-4o и Claude, сгенерировать изображение и оценить качество сервиса.</p>

<p>Это принципиально важно: вы не платите «вслепую». Сначала убеждаетесь, что всё работает — потом принимаете решение о пополнении баланса.</p>

<h2>5. API для разработчиков: OpenAI-совместимый, без VPN</h2>

<p>Если вы разработчик, aineron.ru открывает принципиально новые возможности. Наш API <strong>полностью совместим с форматом OpenAI</strong>: достаточно сменить <code>base_url</code> и <code>api_key</code> в существующем коде — и ваши приложения заработают без VPN, с оплатой в рублях.</p>

<pre><code>import openai

client = openai.OpenAI(
    base_url="https://aineron.ru/api/v1",
    api_key="ak_ваш_ключ",
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Привет!"}],
)
</code></pre>

<p>Поддерживаются: <code>chat/completions</code>, <code>messages</code> (Anthropic-формат), <code>images/generations</code>, <code>embeddings</code>, <code>audio/transcriptions</code>, <code>audio/speech</code>. Есть поддержка стриминга (SSE), пакетной обработки (Batch API) и вебхуков.</p>

<h2>6. Интеграция с IDE: Cursor, Cline, Continue</h2>

<p>Разработчики, использующие AI-ассистентов в IDE, могут подключить aineron.ru как провайдер для <strong>Cursor, Cline и Continue</strong>. Это означает — доступ к GPT-4o и Claude прямо в редакторе кода, без VPN и с рублёвой оплатой.</p>

<h2>7. Командные аккаунты для компаний</h2>

<p>Для юридических лиц и команд доступен B2B-функционал: создание организации, управление участниками и ролями, выставление счетов для безналичной оплаты, общий командный кошелёк, дашборд расходов по участникам и моделям.</p>

<p>Это решает один из главных болей корпоративного использования AI-инструментов: возможность поставить расходы на AI-инструменты под контроль бухгалтерии, получить закрывающие документы и управлять доступом сотрудников централизованно.</p>

<h2>8. Оплата в рублях — без иностранных карт</h2>

<p>Все платежи принимаются через Robokassa: банковские карты российских банков, СБП, интернет-банкинг. Не нужна карта Visa/Mastercard, зарубежный счёт или криптовалюта.</p>

<p>Для юридических лиц — выставление счетов и работа по безналу.</p>

<h2>Итог</h2>

<p>aineron.ru — это не «ещё один AI-чат». Это платформа, которая снимает все российские барьеры доступа к AI: географические (VPN), финансовые (иностранные карты), языковые (интерфейс на русском) и юридические (документы для B2B). При этом технически — это тот же GPT-4o, Claude 3.5, FLUX и десятки других моделей, к которым вы хотите получить доступ.</p>

<p><a href="/register/">Зарегистрируйтесь</a> и получите 10 звёзд бесплатно — никаких карт не нужно.</p>"""

ARTICLE_2_CONTENT = """<p>В мире разработки программного обеспечения давно принят принцип: сложные системы строятся из простых, независимых компонентов. Микросервисная архитектура, Unix-конвейеры, функциональные пайплайны — везде одна идея: <em>делай одно дело хорошо, передавай результат дальше</em>.</p>

<p>Тот же принцип применим к работе с LLM. Когда задача слишком сложна для одного промпта — её нужно разбить на пайплайн промптов, каждый из которых решает ровно одну подзадачу. Это и есть концепция <strong>промпта как микросервиса</strong>.</p>

<h2>Проблема: почему один большой промпт не работает</h2>

<p>Представьте задачу: «Проанализируй этот договор, найди риски для покупателя, переведи ключевые пункты на простой язык и сформируй список вопросов для юриста».</p>

<p>Это четыре совершенно разных когнитивных операции:</p>

<ol>
<li>Юридический анализ (что написано в договоре)</li>
<li>Оценка рисков (что из этого опасно)</li>
<li>Адаптация текста (как объяснить простыми словами)</li>
<li>Генерация вопросов (что уточнить у эксперта)</li>
</ol>

<p>Когда всё это сваливается в один промпт, модель пытается решить все задачи одновременно. Результат получается смешанным: юридический анализ мешается с объяснениями, риски недооцениваются, вопросы для юриста звучат расплывчато.</p>

<p>Исследования (в том числе открытые работы Anthropic и OpenAI) показывают: <strong>LLM работают значительно лучше, когда задача декомпозирована</strong> и каждый шаг сфокусирован на одном типе когнитивной нагрузки.</p>

<h2>Решение: пайплайн промптов</h2>

<p>Принцип прост: вместо одного гигантского промпта создаётся цепочка, где:</p>

<ul>
<li>Каждый промпт получает на вход <em>чётко определённые данные</em></li>
<li>Выполняет <em>одну конкретную трансформацию</em></li>
<li>Отдаёт на выход <em>структурированный результат</em>, который становится входом следующего промпта</li>
</ul>

<p>Для нашего примера с договором пайплайн выглядит так:</p>

<pre><code>Промпт 1 (Извлечение) → JSON со структурой договора
Промпт 2 (Анализ рисков) → JSON с рисками и их серьёзностью
Промпт 3 (Адаптация языка) → Текст на «человеческом» языке
Промпт 4 (Генерация вопросов) → Список вопросов для юриста
</code></pre>

<h2>Пример на Python: анализ конкурентного рынка</h2>

<p>Рассмотрим практический пример: автоматический конкурентный анализ по описанию продукта.</p>

<pre><code>import openai
import json

client = openai.OpenAI(
    base_url="https://aineron.ru/api/v1",
    api_key="ak_ваш_ключ"
)

def run_prompt(system: str, user: str, model="gpt-4o-mini") -> str:
    # Базовый блок пайплайна - один промпт.
    response = client.chat.completions.create(
        model=model,
        messages=[
            {"role": "system", "content": system},
            {"role": "user", "content": user}
        ],
        temperature=0.3,
    )
    return response.choices[0].message.content

# ШАГ 1: Извлечь характеристики продукта
product_description = '''
SaaS-платформа для управления задачами малого бизнеса.
Интеграция с 1С, мобильное приложение, от 590 руб/мес.
'''

features = run_prompt(
    system="Извлеки ключевые характеристики продукта в JSON: "
           "{category, target_audience, key_features: [], price_range, integrations: []}. "
           "Только JSON, без пояснений.",
    user=product_description
)
features_data = json.loads(features)

# ШАГ 2: Определить конкурентов (на основе характеристик)
competitors = run_prompt(
    system="Ты эксперт по российскому SaaS-рынку. "
           "Назови 5 главных конкурентов для продукта с такими характеристиками. "
           "Ответ в JSON: [{name, website, positioning}]. Только JSON.",
    user=json.dumps(features_data, ensure_ascii=False)
)
competitors_data = json.loads(competitors)

# ШАГ 3: Найти слабые места конкурентов
weaknesses = run_prompt(
    system="Проанализируй слабые стороны каждого конкурента с точки зрения "
           "малого бизнеса в России. Для каждого укажи 2-3 конкретных слабости. "
           "JSON: [{competitor_name, weaknesses: []}]. Только JSON.",
    user=json.dumps(competitors_data, ensure_ascii=False)
)
weaknesses_data = json.loads(weaknesses)

# ШАГ 4: Сформировать позиционирование
positioning = run_prompt(
    system="Ты стратег-маркетолог. На основе анализа слабостей конкурентов "
           "предложи уникальное позиционирование нашего продукта. "
           "Текст 3-4 предложения для лендинга.",
    user=f"Наш продукт: {product_description}\n\nСлабости конкурентов: {json.dumps(weaknesses_data, ensure_ascii=False)}"
)

print("=== УНИКАЛЬНОЕ ПОЗИЦИОНИРОВАНИЕ ===")
print(positioning)
</code></pre>

<h2>Ключевые паттерны промпт-пайплайнов</h2>

<h3>1. Паттерн «Структурирование → Трансформация»</h3>

<p>Первый промпт всегда извлекает структуру из неструктурированного текста. Это самый важный шаг — он превращает «сырые данные» в формат, с которым следующие промпты могут работать надёжно.</p>

<p>Правило: промпт-экстрактор должен возвращать <strong>только JSON</strong> без каких-либо пояснений. Это делает парсинг предсказуемым и позволяет легко обрабатывать ошибки.</p>

<h3>2. Паттерн «Специализация модели»</h3>

<p>Разные шаги пайплайна могут использовать разные модели. Простые задачи (извлечение, форматирование) — дешёвые и быстрые модели (GPT-4o mini, Mistral Small). Сложные аналитические шаги — более мощные (GPT-4o, Claude 3.5 Sonnet).</p>

<pre><code>def pipeline(text):
    # Дешёвая модель для извлечения структуры
    structure = run_prompt(..., model="gpt-4o-mini")

    # Мощная модель для глубокого анализа
    analysis = run_prompt(..., model="claude-3-5-sonnet")

    # Снова дешёвая для финального форматирования
    report = run_prompt(..., model="gpt-4o-mini")
    return report
</code></pre>

<p>Такой подход снижает стоимость пайплайна в 3–5 раз по сравнению с использованием GPT-4o на всех шагах, без потери качества результата.</p>

<h3>3. Паттерн «Параллельный разведчик»</h3>

<p>Когда несколько шагов пайплайна независимы друг от друга, их можно выполнять параллельно:</p>

<pre><code>import asyncio

async def parallel_pipeline(document: str):
    # Запускаем три независимых анализа одновременно
    results = await asyncio.gather(
        async_prompt("Найди все даты и дедлайны", document),
        async_prompt("Найди все денежные суммы и обязательства", document),
        async_prompt("Найди все стороны договора и их роли", document),
    )
    dates, amounts, parties = results

    # Объединяем и синтезируем
    summary = await async_prompt(
        "Создай краткое резюме договора",
        f"Даты: {dates}\nСуммы: {amounts}\nСтороны: {parties}"
    )
    return summary
</code></pre>

<p>Это снижает время выполнения в N раз, где N — количество параллельных ветвей.</p>

<h3>4. Паттерн «Критик-исправитель»</h3>

<p>Один из самых мощных паттернов: один промпт генерирует результат, второй — критикует его, третий — исправляет на основе критики.</p>

<pre><code>draft = run_prompt("Напиши email клиенту о задержке проекта", context)
critique = run_prompt(
    "Найди проблемы в этом письме: тон, конкретность, следующие шаги",
    draft
)
final = run_prompt(
    f"Перепиши письмо, исправив следующие проблемы:\n{critique}",
    draft
)
</code></pre>

<p>Этот паттерн особенно эффективен для задач, где качество важнее скорости: деловая переписка, технические описания, маркетинговые тексты.</p>

<h2>Когда разбивать на пайплайн, а когда нет</h2>

<p>Пайплайн оправдан, когда:</p>

<ul>
<li>Задача содержит <strong>принципиально разные когнитивные операции</strong> (извлечение + анализ + генерация)</li>
<li>Выходные данные одного шага нужны как <strong>точные входные данные</strong> для следующего</li>
<li>Важна <strong>отслеживаемость</strong>: вы хотите видеть промежуточные результаты и понимать, где пайплайн сбился</li>
<li>Задачу нужно <strong>масштабировать</strong>: обработать тысячи документов</li>
</ul>

<p>Один промпт достаточен, когда:</p>

<ul>
<li>Задача простая и однородная («переведи этот текст на английский»)</li>
<li>Скорость важнее качества («напиши быстрый черновик»)</li>
<li>Стоимость критична, а результат «достаточно хороший»</li>
</ul>

<h2>Работа с ошибками в пайплайне</h2>

<p>Главная уязвимость пайплайна — сбой на промежуточном шаге. Если второй промпт вернул невалидный JSON, весь пайплайн упадёт. Решение: явная обработка ошибок на каждом шаге.</p>

<pre><code>def safe_json_prompt(system: str, user: str, retries: int = 3) -> dict:
    for attempt in range(retries):
        result = run_prompt(system, user)
        try:
            return json.loads(result)
        except json.JSONDecodeError:
            if attempt == retries - 1:
                raise ValueError(f"Не удалось получить JSON после {retries} попыток")
            # Добавляем явное указание на ошибку в следующую попытку
            system += "\n\nВНИМАНИЕ: Возвращай ТОЛЬКО валидный JSON без каких-либо пояснений."
    return {}
</code></pre>

<h2>Практические советы по построению пайплайнов</h2>

<ol>
<li><strong>Начинайте с конца.</strong> Опишите идеальный финальный результат, потом идите назад — что нужно для предпоследнего шага, что для него — и так до входных данных.</li>
<li><strong>Один выход — один формат.</strong> Промпт-экстракторы — всегда JSON. Промпты-трансформаторы — всегда один конкретный текстовый формат. Не смешивайте.</li>
<li><strong>Логируйте промежуточные результаты.</strong> При отладке это сэкономит часы — вы сразу увидите, на каком шаге пайплайн начал давать некорректные результаты.</li>
<li><strong>Версионируйте промпты.</strong> Храните промпты как код (в файлах или базе данных), не хардкодьте в функциях. Это позволяет итерировать и откатываться.</li>
<li><strong>Тестируйте граничные случаи на каждом шаге.</strong> Что будет, если входные данные пустые? Слишком длинные? На другом языке?</li>
</ol>

<h2>Заключение</h2>

<p>Промпт как микросервис — это не просто метафора. Это архитектурный принцип, который позволяет строить надёжные, масштабируемые и поддерживаемые системы на основе LLM.</p>

<p>Разбивая сложные задачи на цепочку специализированных промптов, вы получаете:</p>
<ul>
<li>Более высокое качество результата</li>
<li>Отслеживаемость и отлаживаемость</li>
<li>Возможность оптимизировать стоимость, выбирая разные модели для разных шагов</li>
<li>Параллелизацию там, где это возможно</li>
</ul>

<p>Попробуйте этот подход на вашей следующей сложной задаче с нейросетями — и вы удивитесь, насколько более управляемым становится процесс.</p>

<p>Для экспериментов с пайплайнами используйте <a href="/api-docs/">API aineron.ru</a> — доступ к GPT-4o, Claude 3.5 и другим моделям без VPN, с оплатой в рублях.</p>"""


class Command(BaseCommand):
    help = 'Создать две SEO-статьи для блога aineron.ru'

    def handle(self, *args, **options):
        # Категории
        dev_cat, _ = Category.objects.get_or_create(
            slug='api-i-razrabotka',
            defaults={
                'name': 'API и разработка',
                'description': 'Гайды по подключению AI API, примеры кода, best practices',
                'seo_title': 'AI API для разработчиков — Гайды и примеры — aineron.ru',
                'seo_description': 'Подключение GPT, Claude, Gemini через API без VPN. Примеры на Python, Node.js, curl.',
            }
        )
        about_cat, _ = Category.objects.get_or_create(
            slug='o-servise',
            defaults={
                'name': 'О сервисе',
                'description': 'Новости, обновления и обзоры aineron.ru',
                'seo_title': 'О сервисе aineron.ru — AI без VPN в России',
                'seo_description': 'Как работает aineron.ru: доступ к нейросетям без VPN с оплатой в рублях.',
            }
        )

        # Статья 1: Преимущества сервиса
        post1, created1 = Post.objects.update_or_create(
            slug='preimushchestva-aineron-nejroseti-bez-vpn',
            defaults={
                'title': 'Aineron.ru: 8 причин выбрать этот сервис для работы с нейросетями в России',
                'category': about_cat,
                'preview_text': 'Полный доступ к GPT-4o, Claude, Gemini и десяткам других моделей без VPN, '
                                'с оплатой в рублях и пробным периодом. Разбираем все преимущества платформы.',
                'content': ARTICLE_1_CONTENT,
                'is_published': True,
                'show_on_main': True,
                'published_at': timezone.now(),
                'seo_title': 'Aineron.ru: Доступ к нейросетям без VPN в России — Преимущества и возможности',
                'seo_description': 'GPT-4o, Claude 3.5, Gemini, FLUX без VPN с оплатой в рублях. '
                                   'Пробный период бесплатно. API для разработчиков. B2B-аккаунты.',
                'seo_keywords': 'нейросети без VPN, GPT-4o в России, Claude API Россия, AI без VPN, '
                                'ChatGPT без VPN, нейросети оплата рублями, aineron.ru',
            }
        )
        action1 = 'создана' if created1 else 'обновлена'
        self.stdout.write(self.style.SUCCESS(f'Статья 1 {action1}: {post1.title}'))

        # Статья 2: Промпт как микросервис
        post2, created2 = Post.objects.update_or_create(
            slug='prompt-kak-mikroservis-pipeline-llm',
            defaults={
                'title': 'Промпт как микросервис: Как разбить сложную задачу на пайплайн LLM',
                'category': dev_cat,
                'preview_text': 'Один большой промпт — почти всегда плохая идея. Узнайте, '
                                'как строить пайплайны промптов по принципу микросервисов: '
                                'надёжно, экономично и масштабируемо.',
                'content': ARTICLE_2_CONTENT,
                'is_published': True,
                'show_on_main': True,
                'published_at': timezone.now(),
                'seo_title': 'Промпт как микросервис: Пайплайн LLM-запросов для сложных задач',
                'seo_description': 'Декомпозиция сложных задач на цепочку промптов: паттерны, '
                                   'примеры на Python, оптимизация стоимости и параллелизация. '
                                   'Архитектурный подход к работе с ChatGPT и Claude.',
                'seo_keywords': 'промпт инжиниринг, LLM пайплайн, chain of prompts, '
                                'prompt chaining, AI разработка, GPT API Python, Claude API',
            }
        )
        action2 = 'создана' if created2 else 'обновлена'
        self.stdout.write(self.style.SUCCESS(f'Статья 2 {action2}: {post2.title}'))
        self.stdout.write(self.style.SUCCESS('Готово! Статьи доступны в /blog/'))
