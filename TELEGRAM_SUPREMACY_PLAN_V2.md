# TELEGRAM SUPREMACY PLAN V2 — аудит вместо наращивания

> Создан: 2026-07-27 | Автор: аудит на живых серверах (aineron.ru + aineron.net), Opus 5 + Sonnet 5, ~2.5 часа проверки.
> Дополняет, не заменяет `TELEGRAM_SUPREMACY_PLAN.md` (S0–S13, все формально «code-complete» на 2026-07-02).
> Метод: не чтение кода вслепую — SSH на оба прод-сервера, синтетические Telegram-апдейты через вебхук,
> живые вызовы `/api/v1/` под QA-ключами, разбор Celery-логов по конкретным message_id, проверка исходников
> Celery/Django в самом контейнере. Каждая находка ниже имеет file:line или воспроизведённый лог/транзакцию.

---

## 0.1 Статус исправлений (обновляется по ходу работы)

**Задеплоено и проверено живьём на проде (оба инстанса, коммиты
`bd3c89b`, `7ad296e`, `19d0d8c`, `6eaa261`):**

- **BUG-C** (невозврат денег за текст) — исправлено и воспроизведено: тот же
  тест, что нашёл баг (модель id=251, 4 попытки, интервал 60с), теперь даёт
  `spend -100 chat:2853` → `refund 100 chat:2853` в `BalanceTransaction`.
  Лог `Возврат средств за проваленную генерацию` появился впервые.
- **BUG-A** (бесплатная регенерация медиа) — исправлено и воспроизведено:
  оригинал + 2 регенерации одного изображения дали три РАЗНЫХ списания
  (`media:2849`, `media-regen:2849:523f...`, `media-regen:2849:59ed...`,
  по 500 коп. каждое) вместо бесплатных повторов.
- **BUG-B** (игнорирование `spend_kopecks()` в медиа) — исправлено тем же
  коммитом, что и BUG-A (общий код-блок).
- **BUG-E** (мёртвая `deepseek-v3`) — деактивирована миграцией `0057` на
  обоих инстансах.
- **BUG-J** (нет periodic tasks на aineron.net) — `setup_periodic_tasks`
  выполнена, 11 задач зарегистрированы.
- **BUG-M** (`menu.py` перехватывал чужой язык и FSM-шаги) — исправлено;
  механизм внедрения `tg_user` в фильтр подтверждён чтением исходника
  aiogram (`HandlerObject.check` → `CallableObject`) прямо в контейнере.
- **BUG-R** (новый, найден при живом тесте BUG-A, не было в исходном
  аудите) — `RegenerateView.post()` падал 500-й ошибкой на ЛЮБОЙ
  регенерации (не только медиа) из-за `error_message = None` в поле без
  `null=True`. Кнопка «Перегенерировать» была полностью нерабочей до
  этого фикса, а не просто «бесплатной».

**Известный побочный эффект тестирования:** сообщение `chat:2851` на QA-
аккаунте `admin@example.com` (aineron.ru) списало 1 ₽ и не было
возвращено — это интерференция ретрай-цепочки с редеплоем ПОСЕРЕДИНЕ
теста (redis/celery_worker пересоздались, отменив запланированный ретрай),
а не признак того, что фикс не работает — тест был перезапущен чисто и
подтвердил исправление. Не является живой находкой, тривиально (1 ₽ на
тестовом аккаунте).

Также задеплоено на оба инстанса (коммиты `bb6cbfe`, `852849b`, `1244280`):

- **BUG-N** (нет глобального `/cancel`, `voice.py` без `StateFilter`) —
  добавлен `/cancel` в `menu.py` (роутер одним из первых), `StateFilter(None)`
  на `handle_voice_message`.
- **BUG-Q** (голос ASR/TTS не тарифицировался) — те же ставки и паттерн
  биллинга (проверка до, списание после успеха), что на вебе в
  `api/views/audio.py`.
- **BUG-P** (неопознанный платёж Stars терялся тихо) — все 6 точек с
  нераспознанным/битым payload или несуществующим tariff_id теперь шлют
  алерт всем `TELEGRAM_ADMIN_IDS` с charge_id для ручного разбора.
- **BUG-F** (дубли цен между `add_new_models.py`/`add_laozhang_models.py`) —
  обнаружено при проверке 17 дублей, не 2, как в исходном аудите; сверено
  с живой БД, дубли убраны из `add_new_models.py`, оставлены только
  7 реально уникальных моделей.

**Ещё не начато / не задеплоено:** BUG-D (`/imgset`), BUG-G (вложения),
BUG-H (доставка картинок >90с), BUG-I (self-serve регистрация), BUG-K
(настройки BotFather), BUG-L (политика конфиденциальности), BUG-O
(секретарь: гонки/троттлинг). См. §1 для деталей каждого.

---

## 0. Вердикт коротко

**Прежде чем добавлять фичи — почините то, что уже продано.** Бот код-комплит на S0–S13,
но:

- **Живого трафика в боте почти нет.** `@aineron_bot` (подтверждено `getMe`, это реальный
  публичный бот, не staging) — **1** запись `TelegramUser` за всю историю, **5** событий
  `TelegramEvent` вообще (все — `onboarding`, 2026-06-24). На aineron.net — тоже 1 `TelegramUser`.
  При этом веб-платформа aineron.ru живая: 119 чатов, 315 сообщений, 6 реальных пользователей.
- **Ни одна из «супер-фич» S4–S8 не тронута ни разу**: `BusinessConnection`, `ManagedBot`,
  `TelegramTopic`, `StarsSubscription` — у всех `count() == 0`. Не «мало использования» — ровно ноль.
- Пока я тестировал, **воспроизвёл вживую на проде критичный денежный баг** (см. §1, находка BUG-1) —
  спишет деньги и не вернёт при сбое генерации текста, механизм подтверждён по исходникам Celery.
- Фоновый аудит кода хендлеров нашёл ещё более серьёзное: **загрузка файлов в бота сломана
  end-to-end** (0 из 46 вложений за всю историю таблицы дошли до модели) и **регенерация
  картинок/видео бесплатна навсегда после первого раза** — это уже не про Telegram, это общий
  движок биллинга, разделяемый с веб-платформой и внешним API.

Вывод: у продукта уже есть широчайший функционал (32+ хендлера, стриминг, rich-сообщения,
AI-задачи, Deep Research, платежи, персоны, память) — **шире, чем у любого проверяемого
конкурента в РФ**. Дефицита фич нет. Есть дефицит: (а) денежной целостности биллинга,
(б) базовой воронки привлечения в самом боте, (в) наблюдаемости (никто не узнал бы про
находки ниже без ручного аудита — в проде нет алертов на такие вещи).

**Решающее правило** (не «нет и точка», а условие пересмотра): возвращаться к фиче-роадмапу
S9+ имеет смысл, когда одновременно (1) самостоятельная регистрация в боте задеплоена и
(2) 4 недели подряд ≥ 50 еженедельно активных пользователей бота. До этого — каждая новая
супер-фича добавляется поверх продукта, которым никто не пользуется, и это неверная ставка
времени независимо от того, насколько фича хороша технически.

---

## 1. Баги — по критерию (деньги под риском) × (охват трафика), не по порядку находки

### КРИТИЧНО — общий биллинговый движок (веб + API + бот, весь трафик платформы)

**BUG-A. [ИСПРАВЛЕНО, задеплоено, см. §0.1] Регенерация изображений/видео бесплатна навсегда после первого раза.**
`src/api/views/chats.py:770,780` — обе проверки баланса/списания обёрнуты в
`if network.provider != 'fal-ai'`, то есть для медиа (`fal-ai`) регенерация **не проверяет
баланс и не списывает** ничего на уровне view, полагаясь на Celery-задачу. Задача же
(`src/aitext/tasks.py:762`) списывает с `reference=f'media:{message.id}'` — той же строкой,
что и при первой генерации. Уникальный индекс `uniq_balance_txn_type_reference`
(`src/users/models.py:1340-1344`) на повторе даёт `IntegrityError`, а
`spend_kopecks()` (`users/models.py:665-680`, **подтверждено чтением исходника**) в блоке
`except IntegrityError` **возвращает `True`, не списав денег** — по дизайну для
идемпотентности ретраев, но здесь используется на операции, которая обязана списывать
заново. Сценарий: сгенерировать картинку → нажать «Перегенерировать» → все последующие
генерации бесплатны, детерминированно, без гонки. Комментарий в
`chats.py:783-785` показывает, что авторы знали про этот класс бага и починили его
uuid-суффиксом в reference — **но только для текстовой ветки**, медиа-ветка эту защиту
не получила.

**BUG-B. [ИСПРАВЛЕНО, задеплоено, см. §0.1] Результат `spend_kopecks()` игнорируется в биллинге медиа.**
`src/aitext/tasks.py:762-763` — возврат функции не проверяется, `stars_deducted = True`
выставляется безусловно, притом что `user` загружен без `select_for_update`
(`tasks.py:720`), а проверка баланса (`:758`) и списание (`:762`) не атомарны как единая
операция. При параллельных медиа-задачах (Celery gevent, concurrency 200 — общая очередь
для всей платформы) проигравший гонку может получить бесплатную генерацию; если она
затем падает, `tasks.py:815` **начисляет возврат денег, которые никогда не списывались** —
баланс растёт из ниоткуда. Общий путь с веб-трафиком, экспозиция — вся платформа.

**BUG-C. [ИСПРАВЛЕНО, задеплоено, см. §0.1] Возврат средств при окончательном сбое генерации текста — мёртвый код.**
Воспроизведено вживую на aineron.ru 2026-07-27 (сообщение id=2847, лог и транзакции
проверены). Путь `/api/v1/chats/` списывает деньги ДО генерации и фиксирует
`billing_reference`/`billing_kopecks` в `message.settings`
(`api/views/chats.py:138,143`). При окончательном провале `generate_ai_response`
(`aitext/tasks.py:1153-1179`) должен сработать возврат через
`except MaxRetriesExceededError: refund_message_billing(message)`. **Это никогда не
происходит**, потому что вызов на `tasks.py:1168` — `raise self.retry(exc=e, countdown=60)`
— передаёт `exc=e`. Исходник `celery.app.task.Task.retry` (проверено прямо в
production-контейнере: `docker exec neiro_web python -c "import inspect,
celery.app.task as t; print(inspect.getsource(t.Task.retry))"`) на ветке исчерпания
попыток делает:
```python
if max_retries is not None and retries > max_retries:
    if exc:
        raise_with_context(exc)          # <-- наш случай: exc=e передан явно
    raise self.MaxRetriesExceededError(...)
```
То есть при `exc` задан — Celery **всегда** ре-рейзит исходное исключение вместо
`MaxRetriesExceededError`, и блок `except MaxRetriesExceededError:` с возвратом денег
недостижим. Живое воспроизведение: сообщение 2847, модель `deepseek-v3` (см. BUG-E ниже),
4 попытки (13:28:46 → 13:32:01, интервалы ровно 60 с), после четвёртой — ни строки
`Возврат средств`, ни `Не удалось вернуть средства`, ни пятой попытки: задача просто
остановилась. Баланс аккаунта `admin@example.com` не изменился (5947 коп. до и после).
Контраст: у медиа-ветки (`tasks.py:811-825`) возврат синхронный и безусловный в самом
`except`, без зависимости от Celery-ретраев — там это работает (подтверждено историей
транзакций: `spend -2500 media:2841` → `refund 2500 media:2841`, разница 5 мс).
**Фикс:** либо не передавать `exc=e` в `self.retry(...)` (тогда Celery сам поднимет
`MaxRetriesExceededError` на исчерпании), либо перенести возврат в блок, который
гарантированно достижим независимо от того, какое исключение долетело.
Сумма по одному инциденту невелика (цена сообщения, обычно 1–4 ₽), но паттерн системный:
любой permanent-failure текстовой генерации (мёртвая модель, невалидный запрос,
контент-политика без явной проверки) бьёт по этому же мёртвому коду.

**BUG-D. `/image` в боте не имеет модели данных для настроек вообще.**
У `TelegramUser` есть поле `video_settings` (`telegram_bot/models.py:65`), поля
`image_settings` **не существует**. `images.py:46` создаёт сообщение без `settings=`
(в отличие от `video_cmd.py:40-41`), поэтому в `tasks.py:745` условие `if user_settings:`
для `/image` всегда ложно — генерация всегда идёт на голых `api_defaults`. Из бота
физически нельзя купить: `DALL-E 3 quality=hd` (+10 ₽), `GPT Image quality=high`
(+10 ₽), `Flux num_images=2/4` (+20 ₽ / +60 ₽) — рабочие платные апселлы на сайте.
Это единственное место в этом отчёте, где «добавить фичу» напрямую даёт выручку и
небольшая по объёму работа — но даже адаптация существующего `/videoset`
(`video_settings_cmd.py:36`, `_editable_fields` принимает только `select`/`checkbox`)
не покроет всё: у image-моделей встречаются ещё `number` (seed) и `textarea`
(negative_prompt) — нужна доработка типов полей, не только копипаста команды.

**BUG-E. [ИСПРАВЛЕНО, задеплоено, см. §0.1] Мёртвая модель активна в каталоге прямо сейчас.**
`DeepSeek V3` (`model_name='deepseek-v3'`, id=251, `is_active=True`) отклоняется и
laozhang.ai (503 «no available channels»), и apimart.ai в фолбэке (400 «not a valid
model ID») — то есть fallback-механизм (`AI_PROVIDER_FALLBACK=1`) сработал корректно
(подтверждает исправность инфраструктуры фолбэка из [[project_provider_fallback_b13]]),
но у самой модели нет рабочего провайдера ни на одном плече. Активна на обоих
инстансах. Любой пользователь, выбравший её для текстового чата, попадает в BUG-C.
Рядом в каталоге есть рабочие `deepseek-v3.1` (id=507) и `deepseek-v3.2` (id=539) —
похоже на забытый дубль после апгрейда версии.

**BUG-F. [ИСПРАВЛЕНО, задеплоено, см. §0.1] Дублирующиеся команды сидинга моделей расходятся в цене.**
`add_laozhang_models.py` и `add_new_models.py` оба сидят через `update_or_create(slug=...)`
одни и те же слаги (`seedream-5-0`, `seedream-4-5`, `seedream-4-0`, `gemini-3-1-flash-image`,
`gemini-3-pro-image`, `gemini-2-5-flash-image`, `gpt-image-1-5`) с разным
`cost_per_message` (Seedream 5.0: 11 против 40 ₽; GPT Image 1.5: 13 против 35 ₽).
Реальная цена в проде зависит от того, какая команда запускалась последней — не от
прайсинг-решения. Нужно свести к одному источнику истины на модель.

### КРИТИЧНО / БЛОКЕР ЗАПУСКА — специфично для бота

**BUG-G. Загрузка файлов и фото в бота сломана целиком.**
Подтверждено данными, не только кодом: на aineron.ru **46 из 46** записей
`FileAttachment` за всю историю таблицы имеют `message IS NULL` — то есть ни одно
загруженное вложение никогда не было привязано к сообщению. Причина в коде:
`telegram_bot/handlers/chat.py:118` объявляет параметр `process_text(...,
attachment=None, ...)`, но нигде в теле функции его не использует; вызовы из
`files.py:100,184,340` передают `attachment=att`, он молча теряется. `files.py:28`
создаёт `FileAttachment(message=None, ...)`, извлечённый текст пишется в
`att.extracted_text`, но `user_msg.attachments.all()` (обратный FK, читается в
`tasks.py:944`) для непривязанных вложений всегда пуст. **Итог: пользователь
отправляет боту PDF или фото, платит за ответ, модель получает только шаблонную
фразу вроде «Опиши это изображение подробно» без единого байта контента и
отвечает выдумкой** — тихая, недорогая по деньгам, но репутационно опасная порча
качества. На веб-платформе аналогичная привязка происходит явно и в другом месте
(`api/views/chats.py:146-149`, через `attachment_ids`) — там механизм иной и это не
проверялось в рамках этого аудита как отдельная гипотеза.

**BUG-H. Картинки, сгенерированные дольше 90 секунд, не долетают до бота как картинки.**
`images.py:41-45` создаёт `Chat` **без** `settings={'telegram_chat_id': ...}`, тогда как
`video_cmd.py:36` и `img2video_cmd.py:80` этот ключ ставят. Ветвление доставки в
`tasks.py:796-806` идёт ровно по этому полю: без него вместо `send_media_to_telegram`
срабатывает `maybe_notify` — обычный текст со ссылкой на сайт. При этом бот-сторонний
поллинг (`images.py`, `POLL_MAX_TRIES=30 × POLL_INTERVAL=3` = 90 секунд) к этому
моменту уже написал пользователю «таймаут». Итог: деньги списаны, бот сказал
«не получилось», а результат на самом деле есть — просто пришёл голой ссылкой после
того, как разговор в голове пользователя уже закрылся на «не сработало». Тот же
дефект в `img2img_cmd.py:60-64`.

### ВЫСОКО

**BUG-I. Нет самостоятельной регистрации в боте.**
`start.py:73-98,166-169` — голый `/start` без токена привязки **не создаёт
аккаунт**, а прямым текстом инструктирует: «1. Перейдите на aineron.ru
2. Кабинет → Telegram → Подключить 3. Вернитесь сюда по ссылке» (см.
`locales/ru.json: start.introBody`). Это не баг в смысле «сломано», это
осознанная архитектура — но она означает, что бот физически не может
привлекать новых пользователей сам по себе: находка нового человека бота
через поиск в Telegram, рекламу, пересылку от друга — заканчивается требованием
сначала завести веб-аккаунт. При том что весь остальной рынок (см. §3) даёт
рабочий чат сразу после `/start`. Это, по всей вероятности, главная структурная
причина, почему при живой веб-платформе (6 юзеров, 315 сообщений) в боте
всего 1 реальный пользователь.

**BUG-J. [ИСПРАВЛЕНО, задеплоено, см. §0.1] На aineron.net не зарегистрирована ни одна периодическая задача бота.**
`django_celery_beat.PeriodicTask` на aineron.net содержит только Studio/sandbox-задачи —
ни `run_due_ai_tasks`, ни `send_reminders`, ни `send_daily_digests`, ни
`notify_low_balance`, ни `cleanup_business_drafts`, ни `business_daily_summary`
(на aineron.ru все они есть и включены). Причина — `manage.py setup_periodic_tasks`
никогда не запускалась на этом сервере. Сейчас не аварийно (0 активных `AITask`,
1 пользователь), но это готовая мина: если кто-то создаст AI-задачу или напоминание
через веб `/account/tasks/` (не завязано на INTL-флаг), она молча никогда не
выполнится, а черновики AI-секретаря будут копиться без TTL-очистки.
**Обязательный шаг перед любым маркетингом на aineron.net.**

**BUG-K. Рассинхронизация фиче-флагов и реальных возможностей бота в Telegram.**
На aineron.ru включены `TG_BUSINESS=1`, `TG_TOPICS=1`, `TG_MANAGED_BOTS=1` — но
`getMe` того же бота возвращает `can_connect_to_business: false`,
`has_topics_enabled: false`, `can_manage_bots: false`. Это отдельный уровень
настроек в BotFather (`/setbusiness`, темы для личных чатов, `getManagedBotToken`
доступ), не покрываемый нашими env-флагами — `capabilities.py` проверяет только
наличие метода в SDK, не фактическую разрешённость на стороне Telegram. Итог:
код готов, флаг включён, а сама фича физически не активируется ни для одного
пользователя, пока это не настроено в BotFather. Нигде в коде это не
диагностируется — без ручной проверки `getMe` никто бы не узнал.

**BUG-L. Политика конфиденциальности не упоминает Telegram Business вообще.**
`LegalDocument(document_type='privacy')` (обновлена 2026-04-06, до того как
AI-секретарь был спроектирован в июле) не содержит ни слова «Business», «бизнес»,
«секретар», «клиент». При этом код секретаря (`business.py:154-160`) хранит
полный текст входящих сообщений клиента (`incoming_text`), имя и chat_id — и
докстринг в самом коде (`business.py:10-11`, `models.py:494-495`) утверждает
«переписка клиентов не логируется», что фактически неверно (TTL 7 дней — есть,
но это не то же самое, что «не логируется»). Риск уже был явно описан в
исходном плане (§5.4) как условие включения `TG_BUSINESS`, но флаг включён,
а условие не выполнено. **Рекомендация: выключить `TG_BUSINESS` до обновления
политики конфиденциальности**, даже притом что `BusinessConnection.count() == 0`
и реального риска для живых людей пока нет — это вопрос готовности к моменту,
когда первый пользователь подключится.

### СРЕДНЕ

**BUG-M. [ИСПРАВЛЕНО, задеплоено, см. §0.1] `menu.py` перехватывает обычные сообщения без учёта FSM/языка.**
`menu.py:41` — `@router.message(F.text.func(lambda text: text in
_all_menu_labels()))`, роутер подключён вторым (`bot.py:87`), раньше
`chat.router`. `_all_menu_labels()` объединяет подписи кнопок **по всем языкам**
(`ru.json`, `en.json` и т.д.) в одно множество. Результат: пользователь, который
просто пишет боту одно слово «Chat» или «Help» (по-английски, даже если у него
русская локаль) — получает статичную карточку меню вместо ответа нейросети;
сообщение до AI не доходит вообще. Тот же роутер без `StateFilter(None)`
перехватывает и текстовые шаги FSM (например, ввод текста для напоминания),
если пользователь в этот момент нажал кнопку меню — состояние не сбрасывается,
следующее сообщение уходит не туда. Дёшево чинится: добавить `StateFilter(None)`
и убрать пересечение локалей (сравнивать только с текущим языком пользователя).

**BUG-N. [ИСПРАВЛЕНО, задеплоено, см. §0.1] Нет глобального выхода из FSM.**
`start.py:74` не вызывает `state.clear()`, единого `/cancel` нет — команда
работает только точечно в паре мест (`settings_cmd.py:97`,
`channel_cmd.py:246-247`). Практический сценарий: пользователь в шаге
«введите текст напоминания» пишет `/cancel`, ожидая отмены — вместо этого
создаётся напоминание с текстом «/cancel». `voice.py:51` (голосовые/видео-заметки)
тоже без `StateFilter`, подключён раньше нескольких FSM-роутеров — голосовое
сообщение внутри любого мастера уйдёт в обычный чат с полным списанием, а
мастер не завершится.

**BUG-O. AI-секретарь: тихие траты и гонки, помимо приватности из BUG-L.**
`can_reply`, сохранённый на подключении (`business.py:277`), нигде не
проверяется перед автоответом (`:325`, `:252`) — если у подключения нет прав
на ответ, каждое сообщение клиента даёт платный вызов LLM, попытку отправки,
неудачу и возврат денег (деньги возвращаются, токены провайдера — нет).
Счётчик бесплатной квоты секретаря (`business.py:195`) читает значение до
атомарного инкремента — два одновременных клиента могут оба пройти бесплатный
лимит. Троттлинга и дневного капа на автопилот нет вообще (`on_business_message`,
`:316`) — при `scope_all` цепочка сообщений от одного клиента может выкачать
баланс владельца без верхней границы.

**BUG-P. [ИСПРАВЛЕНО, задеплоено, см. §0.1] Неопознанный платёж — деньги у Telegram, баланс не пополнен, тишина.**
`payment.py:59-61` подтверждает `pre_checkout` без валидации payload; при
неизвестном payload на успешном платеже (`:304-305`, `:346-347`) код просто
логирует warning и выходит — без начисления, без возврата, без сигнала в
поддержку. Могло сработать на устаревшем инвойсе после смены формата пакетов.

**BUG-Q. [ИСПРАВЛЕНО, задеплоено, см. §0.1] Голосовые ASR/TTS не тарифицируются вообще.**
`voice.py:18` (Whisper) и `voice.py:34` (TTS) вызываются без проверки баланса
и без списания — расход у провайдера идёт всегда, оплата — никогда. Кнопку TTS
(`voice.py:83`) можно жать многократно на одном сообщении.

### НИЗКО (зафиксировать, не приоритет)

- Подпись к видео показывает базовую цену без доплаты за настройки `/videoset`
  (`video_cmd.py:98` vs `tasks.py:800`) — расхождение витрины и факта списания.
- `UserSpending.amount = max(1, cost // 100)` округляет суб-рублёвые расходы
  вверх в аналитике.
- Debug-конфигурация `app.conf.beat_schedule` в `config/celery.py:17-28`
  прописывает `process-pending-renewals`/`notify-upcoming-expiration` на
  **каждую минуту** с комментарием «ВРЕМЕННО для тестирования», хотя
  CLAUDE.md документирует «каждые 12 ч» / «один раз». В обоих задачах есть
  идемпотентные guard'ы (проверено по коду) — двойного списания или спама
  уведомлений это не даёт, но 1440 лишних DB-запросов/день и рассинхрон с
  документацией стоит убрать.
- `setup_webhook.py:59` включает `successful_payment` в `allowed_updates` —
  это поле объекта `Message`, а не отдельный тип апдейта; вероятно избыточно
  (не проверялось, разрывает ли это `set_webhook`, но проверяется одной
  командой: `manage.py setup_webhook --info`).
- Нет лимита размера файла для фото (`files.py: handle_photo`, в отличие от
  документов с лимитом 20 МБ); `voice.py` читает голосовые целиком в память.

### Проверено — регрессий не нашли

`auto_renew=False` для Stars-подписок держится (`payment.py:117-119`), не
попадает обратно в Robokassa-цикл — прошлый критичный баг из аудита S0–S8 не
вернулся. XTR-платежи идемпотентны по `charge_id`. `StateFilter(None)` на
собственном catch-all чата (`chat.py:402`) на месте. TTL черновиков секретаря
(7 дней) реально запланирован и выполняется (`setup_periodic_tasks.py:41-45`).
`/videoset` действительно применяется и в `/video`, и в `/img2video`. Медиа-ветка
`generate_ai_response` не ретраится через Celery (`return` внутри `except`),
поэтому хотя бы для *первой* генерации двойного списания через ретраи нет.

---

## 2. Что делать вместо наращивания фич — в порядке

1. **Остановить кровотечение денег.** BUG-A/B/C/E/F — это не Telegram-специфика,
   это общий биллинговый движок под всем платным трафиком платформы. Чинить
   первыми, независимо от дальнейшей судьбы бота.
2. **Мониторинг вместо ручного аудита.** Ни один из багов A–P не был бы виден
   без SSH и чтения логов вручную. Минимум: алерт на `MaxRetriesExceededError`/
   финальный провал `generate_ai_response`, дашборд «списано без результата»
   (spend без соответствующего completed/refund в разумное окно), еженедельная
   сверка `is_active`-моделей против пробного вызова каждой (поймало бы BUG-E
   за 5 минут вместо случайного обнаружения).
3. **Самостоятельная регистрация в боте (BUG-I).** Технически: `/start` без
   токена создаёт `CustomUser` + `TelegramUser` напрямую (email не обязателен
   для Telegram-native аккаунта, привязка к вебу — опционально позже через тот
   же `TelegramLinkToken`, но не как единственный путь входа). Это отдельный от
   S9–S13 продуктовый спринт, и по влиянию на будущий рост бота он важнее любой
   фичи из старого плана — без него у бота нет собственного канала привлечения.
4. **Починить BUG-G/H** до любого публичного анонса бота — иначе первые же
   органические пользователи столкнутся с «бот не видит мои файлы» и
   «сказал таймаут, а картинка была».
5. **`setup_periodic_tasks` на aineron.net (BUG-J)** — блокер перед любым
   маркетингом на международном инстансе, 10 минут работы.
6. **BUG-D (`/imgset`)** — единственный пункт в этом списке, который buys
   revenue напрямую и стоит недорого; делать вместе с остальными фиксами,
   не как отдельный «будущий спринт».
7. Golden-path ручной прогон в реальном Telegram-клиенте — см. §5, нужен
   пользователь.

---

## 3. Рынок — что реально удалось проверить (агент Opus 5, WebSearch, июль 2026)

**Подтверждено источниками.** Русскоязычный рынок (DTF-каталог, 13 ботов:
BotHub, ChadGPT/chadgpt.ru, GPTunnel, СИНТАКС, GigaChat и др.) конкурирует по
четырём осям: широта каталога моделей, бесплатный стартовый лимит,
**невыгорающие pay-per-use пакеты**, видимая цена до отправки запроса.
Единственный проверяемо крупный игрок — **GigaChat (~3 млн MAU)** — выигрывает
за счёт первопартийной дистрибуции Сбера (обязательный Sber ID), не глубины
фич. Ни один из 13 описанных ботов не упоминает задачи по расписанию,
AI-секретаря, топики-проекты или sub-bots — эти оси в рыночном нарративе
середины 2026 просто отсутствуют, ни как чужое преимущество, ни как запрос
пользователей.

**Данных недостаточно** (честно, не выдумано): по конкретным лидирующим
AI-ботам в Индонезии, Турции, на Ближнем Востоке — публичный веб-поиск не дал
проверяемых названий и цифр, кроме размера самой Telegram-аудитории региона
(Индонезия ~27 млн активных пользователей Telegram). Сравнение in-bot UX
(настройки картинок, онбординг, рефералка) конкурентов тоже не проверяемо
по открытым источникам без ручного прогона их ботов вживую.

**Вывод для решения:** данных достаточно, чтобы не подстраивать фиче-роадмап
под фантомных конкурентов. Единственный проверяемый сигнал масштаба
(GigaChat) говорит про дистрибуцию, а не про фичи, что напрямую поддерживает
приоритет §2 (воронка привлечения) над §4 (новые супер-фичи).

---

## 4. Фиче-роадмап — коротко, в конец, после условия из §0

Технически S9–S13 из старого плана остаются валидными идеями (Agent Mode,
голос-в-голос, канал-мейкер, маркетплейс персон, i18n) — код для многих уже
есть. Ничего из них не переделывать и не выкидывать. Просто: **не начинать
новый спринт из этого списка, пока не выполнено условие реверса из §0**
(self-serve регистрация в бою + 4 недели ≥ 50 WAU бота подряд). Если условие
выполнится — сначала S13 (i18n для aineron.net, где инфраструктура для роста
уже частично готова: 5 языков локализации есть, `TELEGRAM_BOT_USERNAME=
aineron_global_bot` уже отдельный от RU), потом остальное по прежнему
приоритету старого плана.

---

## 5. Что нужно от пользователя — golden path вживую

Автоматически через синтетические Telegram-апдейты можно проверить только
логику до финального `sendMessage` (реальная доставка требует настоящего
Telegram-клиента, который начал диалог с ботом). Что стоит прогнать вручную
в `@aineron_bot`, читая логи `docker logs neiro_celery_worker -f` на
`aineron-ru` параллельно:

1. `/start` с deep-link из кабинета (Аккаунт → Telegram → Подключить) — базовая привязка.
2. Обычное текстовое сообщение — проверить, что стриминг/rich-ответ выглядит нормально.
3. Отправить фото с подписью «опиши это» — ключевая проверка BUG-G после фикса.
4. `/image <промт>`, затем `/videoset` → `/video <промт>` — сравнить UX настроек.
5. Нажать «Перегенерировать» на готовой картинке дважды подряд — проверка BUG-A после фикса.
6. `/balance`, `/task` (создать AI-задачу), `/remind` — и написать `/cancel` внутри мастера — проверка BUG-N.
7. Голосовое сообщение внутри мастера `/remind` — проверка BUG-N/voice.
8. Написать боту просто «Chat» или «Help» одним словом — проверка BUG-M.

---

## 6. Источники находок

Прямой SSH-аудит (`aineron-ru`, `aineron-net`): `.env`, `docker logs`,
`django_celery_beat.PeriodicTask`, `BalanceTransaction`, `Message`, чтение
исходника `celery.app.task.Task.retry` и `users/models.py` в контейнере.
Синтетический POST на `/telegram/webhook/` с `X-Telegram-Bot-Api-Secret-Token`.
Живые вызовы `/api/v1/chats/` под persistent QA-ключами (см. память
`reference_qa_credentials`) — воспроизведение BUG-C и живая проверка
генерации изображения (модель `z-image-turbo`, успех за 15 с). Фоновый агент
Opus 5 — построчный аудит `handlers/*.py`, `tasks.py`, `billing.py`,
`business.py`, `payment.py`, `bot.py` (порядок роутеров). Отдельный агент
Opus 5 — рыночное исследование с явным разделением подтверждённого и
оценочного.
